Perfect. Let's add another detail:

1. Data Fetching Strategy
   . React Query (@tanstack/react-query) is required for all API calls - provides caching, loading states, and error handling
   . Axios for HTTP client in service layer

2. State Management
   . Use React Query for server state (API data)
   . Use React hooks (useState, useContext) for UI state
   . Use global state library (Redux/Zustand) if needed for this project

3. Styling
   . use the font type that is most suitable for this platform

What do you think ? you can review it also and add another suggestion
